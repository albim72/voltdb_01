curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"

sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl

kubectl version --client


update UBUNTU

sudo apt update
sudo apt install -y curl ca-certificates

curl --version



Zrób teraz instalację Minikube.

Zainstaluj curl, jeśli go nie ma
sudo apt update
sudo apt install -y curl

Pobierz i zainstaluj Minikube

Dla Ubuntu/Linux x86-64:

curl -LO https://github.com/kubernetes/minikube/releases/latest/download/minikube-linux-amd64
sudo install minikube-linux-amd64 /usr/local/bin/minikube
rm minikube-linux-amd64

To jest oficjalna metoda instalacji Minikube dla Linux x86-64.

Sprawdź instalację -> 
minikube version
which minikube

Powinieneś zobaczyć coś w rodzaju:

/usr/local/bin/minikube
Uruchom klaster na Dockerze -> 
minikube start --driver=docker

Docker driver jest oficjalnie wspierany na Linuxie i pozwala uruchomić Kubernetes wewnątrz istniejącego Dockera.

klaster działa?

kubectl config get-contexts
kubectl get nodes
kubectl cluster-info

Poprawny wynik dla kubectl get nodes powinien wyglądać mniej więcej tak:

NAME       STATUS   ROLES           AGE   VERSION
minikube   Ready    control-plane   ...   ...
Jeśli pojawi się błąd permission denied przy Dockerze

Wtedy wpisz:

sudo usermod -aG docker $USER
newgrp docker
docker ps

I ponownie:

minikube start --driver=docker

PRZESTRZENIE NAZW
NAMESPECES

kubectl get namespaces

UTWORZENIE NAMESPACE SZKOLENIOWEGO
kubectl create namespace voltdb-lab

ustawienie przestrzeni domyslmnej
kubectl config set-context --current --namespace=voltdb-lab

sprawdzenie
kubectl config view --minify | grep namespace

sprawdzenie wszystkch pods 
kubectl get pods --all-namespaces
kubectl get nodes

kubectl get pods -A | grep -i volt







